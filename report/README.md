# Report

- `main.tex` – publication-style lab report (course formatting constraints)
- `main.pdf` – compiled PDF (for quick viewing)
- `images/` – figures used in the report (**provided lab materials only**)

To compile locally:

```bash
pdflatex -interaction=nonstopmode main.tex
pdflatex -interaction=nonstopmode main.tex
```

Or upload the entire `report/` folder to Overleaf.
